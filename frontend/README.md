# secure-ai-docs - Frontend

Vite + React + TypeScript frontend. Files are placeholders and ready for implementation.

To run locally (after installing dependencies):

```powershell
npm install
npm run dev
```
